# QuickFile-Go

QuickFile-Go 是一个基于 Go 后端和 LuCI 前端的 OpenWrt 文件管理插件。

## 特点

- 不依赖 nginx 代理。
- 不依赖原版 quickfile 预编译后端。
- Go 后端源码在 `src/main.go`，LuCI 前端源码在 `htdocs/luci-static/resources/view/quickfile-go.js`。
- 后端默认监听 `0.0.0.0:8989`，浏览器从 LuCI 页面直接请求 Go API。
- 后端会校验 LuCI session，未登录 LuCI 时不能调用 API。
- 高风险操作使用 POST。
- 下载接口支持 LuCI session token 校验。
- CORS 限制为同主机来源，不使用 `Access-Control-Allow-Origin: *`。
- 支持 `/etc/config/quickfile-go` 配置化管理。

## 已支持功能

- 浏览目录
- 上传文件
- 在线下载 URL 到当前目录
- 下载文件
- 新建文件 / 文件夹
- 删除
- 重命名
- 复制 / 剪切 / 粘贴
- 图片 / 视频预览
- Monaco Editor 编辑体验；无法加载 Monaco 时自动回退到内置编辑器
- `.apk` / `.ipk` 安装
- 真正实时终端：本地内置 xterm.js + 后端 `/term` WebSocket + Linux PTY
- 终端开关配置化
- 监听地址 / 监听端口配置化
- 最大上传大小 / 最大编辑大小配置化，`0` 表示不限制
- 诊断信息接口和 LuCI 设置区
- 当前目录搜索
- 网格 / 列表视图
- 深色 / 浅色模式
- `.tar.gz` / `.tar.xz` / `.zip` 压缩
- `.zip` / `.tar.gz` / `.tgz` / `.tar.xz` / `.txz` / `.tar` / 单文件 `.gz` 解压

## UCI 配置

默认配置文件：

```sh
/etc/config/quickfile-go
```

默认内容：

```uci
config quickfile-go 'main'
	option enabled '1'
	option listen_addr '0.0.0.0'
	option listen_port '8989'
	option enable_terminal '1'
	option max_upload_mb '0'
	option max_edit_mb '0'
```

说明：

- `enabled`: 是否启动 quickfile-go 服务。
- `listen_addr`: 监听地址，默认 `0.0.0.0`。
- `listen_port`: 监听端口，默认 `8989`。
- `enable_terminal`: 是否启用实时终端。
- `max_upload_mb`: 最大上传大小，单位 MiB，`0` 表示不限制。
- `max_edit_mb`: 最大读取/保存编辑文件大小，单位 MiB，`0` 表示不限制。

修改配置后重启服务：

```sh
/etc/init.d/quickfile-go restart
```

也可以在 LuCI 页面里的 “设置 / 诊断” 区域保存并重启。

## 安全说明

QuickFile-Go 不使用 nginx 反代，因此 API 端口 `8989` 会在路由器上监听。请确保不要把该端口开放到 WAN。

建议：

- 不要添加 WAN 允许访问 `8989` 的防火墙规则。
- 只在可信 LAN 环境使用。
- 保持 LuCI 登录密码强度。
- 不需要终端功能时，把 `enable_terminal` 设置为 `0`。
- 若把监听端口从 `8989` 改成其他端口，当前浏览器会记录新端口；其他浏览器首次访问时仍可能需要在 URL 后加 `?qfport=新端口`。

## Monaco / 终端说明

r43 已内置 Monaco Editor 本地静态资源：

- 编译安装后 Monaco 位于：`/www/luci-static/resources/quickfile-go/monaco/vs/loader.js`
- 编辑器只加载本地 Monaco，不请求 CDN
- Monaco 加载成功时提供代码高亮、行号、搜索、自动布局、`Ctrl+S` 保存
- Monaco 异常或资源缺失时，会自动回退到内置暗色编辑器，不会卡在加载界面

r48 起终端使用本地内置 xterm.js 资源，不请求 CDN。编译安装后资源位于：

```text
/www/luci-static/resources/quickfile-go/xterm/
```

前端终端由 xterm.js 渲染，后端仍然是 `/term` WebSocket + Linux PTY，因此全屏程序、ANSI 显示、复制粘贴体验比早期手写终端更接近原版 quickfile。

终端复制/粘贴支持：

终端标题栏提供“复制 / 粘贴 / 清屏”按钮。


- 鼠标选中文本后可用浏览器菜单或 `Ctrl+C` 复制。
- 无选中文本时，`Ctrl+C` 仍然发送中断信号。
- `Ctrl+V` 或右键菜单粘贴会把剪贴板内容发送到终端。
- `Ctrl+Shift+C / Ctrl+Shift+V` 也支持复制/粘贴。

## 诊断接口

LuCI 页面提供“诊断”按钮。后端接口为：

```text
/api?action=diagnose
```

诊断信息包括：

- 版本号
- 运行状态
- PID
- 运行时间
- 监听地址/端口
- 终端是否启用
- 最大上传/编辑限制
- shell / xz / apk / opkg / ubus / uci 工具是否存在

## tar.xz 说明

`.tar.xz` 压缩/解压通过系统 `xz` 命令完成。如果系统没有 `xz`，操作会返回明确错误。可以在路由器上安装相应 xz 工具包后再使用。

## 编译

放到 OpenWrt 源码树后编译：

```sh
make package/luci-app-quickfile-go/compile V=s
```

如果你的源码树路径是 `package/network/luci-app-quickfile-go`，按实际路径调整 make 目标。

## 安装后清理 LuCI 缓存

```sh
rm -f /tmp/luci-indexcache /tmp/luci-modulecache/* 2>/dev/null
/etc/init.d/uhttpd restart
```

## 许可证

Apache License 2.0。详见 `LICENSE`。

## 版本说明

### r39/r40 增强与审计

r39 新增上传进度条、在线下载后台任务和进度查询、文件属性接口和属性面板、右键复制路径/修改权限/查看属性，以及列表模式的大小、修改时间、权限、所有者列和排序。

r40 清理旧的同步 `remote_download` 接口和旧的 `exec` 单命令接口；终端只保留 PTY WebSocket 实时终端；同时修复上传错误响应、CORS 重复 header、大文件操作超时等问题。

### r41 大文件上传

r41 将上传接口改为流式写入，避免 Go `ParseMultipartForm` 先把大文件落到 `/tmp`。上传 2GB 这类大文件时，请确认目标目录所在分区空间足够；OpenWrt 的 `/tmp` 通常是内存 tmpfs，不适合保存大文件。

### r43/r44/r45 本地 Monaco 与审计清理

r43 起源码包内置 Monaco Editor 本地静态资源，编译安装后会放到：

```text
/www/luci-static/resources/quickfile-go/monaco/vs/loader.js
```

文件编辑器只加载本地 Monaco，不请求 CDN。Monaco 资源异常时仍会自动回退到内置暗色编辑器。

r44 修复 UCI 配置文件漏打包导致的编译失败。r45 继续清理 README 历史残留、前端重复 CSS、xterm 占位说明，并复核 Makefile、UCI、init 和 Monaco 打包路径。


## Monaco 精简版说明

r46 内置的是精简 Monaco：保留常用配置/脚本语言高亮和核心编辑体验，删除大型 TypeScript worker、多余 NLS 和不常用语言定义，以降低固件体积。若需要完整 Monaco，可替换 `htdocs/luci-static/resources/quickfile-go/monaco/vs/` 为完整 Monaco `min/vs` 树。


## r48

- 内置本地 xterm.js 终端资源。
- 复制、移动、压缩、解压改为后台任务模式。


## r50 后台下载 UI

在线下载会创建后台任务，不再弹出覆盖文件列表的进度遮罩。下载进度请点击右上角“任务”查看，下载期间可以继续浏览、编辑、上传或执行其他文件操作。


## r51 审计修复

r51 进行全量审计后修复：

- 压缩/解压任务支持更及时的取消检查；Go 原生 tar/zip/gzip 读写循环会响应任务取消。
- 修复任务表超过上限时可能误清理正在运行任务的历史问题。现在只清理已结束的旧任务，并同步清理 cancel 句柄。
- 修正 fallback 写入 `/etc/config/quickfile-go` 的权限为 `0600`。
- 同步 README 里关于本地 xterm.js 终端的说明，清理 r38 旧终端描述残留。


## r52 性能优化

- OpenWrt Go 构建加入 `-ldflags "-s -w"`，减少后端二进制符号和调试信息体积。
- 大文件上传/远程下载/归档读写缓冲提升到 1MiB，减少系统调用次数，更适合 x86/PVE 场景。
- 上传进度 UI 做节流，减少大文件上传时浏览器频繁重绘。
- 后台任务轮询间隔降低频率，复制/移动/压缩/解压默认进入后台任务中心，避免遮罩阻塞文件管理页面。
- Monaco/xterm 本地资源带版本参数，浏览器可缓存同版本资源，升级后自动刷新资源。

## r70 安装 UI 与自升级稳定性修复

- 修复通过 QuickFile-Go 安装/升级 QuickFile-Go 自身时，postinst 立即重启后台导致安装日志连接中断的问题；现在重启会延迟并脱离安装命令执行。
- 补齐浅色模式下安装日志弹窗内通用按钮的颜色，避免按钮残留深色块。
- 清理前端注释里的旧版本号残留，降低后续维护误判。


## r71 文件类型图标优化

- 优化文件列表图标：`.txt`、`.apk/.ipk`、压缩包、配置文件、代码文件、PDF、音视频等不再共用同一个默认图标。
- 新增按扩展名生成的彩色文件类型图标，列表中更容易区分软件包、文本、配置、压缩包等文件。
- 保留图片缩略图逻辑，图片仍优先显示真实缩略图。
- 修复部分常见文本/配置文件点击后无动作的问题：新增 `.cfg`、`.leases`、`.list`、`.ts`、`.csv`、`.bash/.zsh` 等自动文本编辑识别。


## r72 文件图标样式重做

- 重做文件图标绘制逻辑，不再只是同一张文件底图 + 不同标签。
- 为不同类别提供明显不同的视觉样式：
  - 软件包：箱子/包裹风格
  - 压缩包：拉链包风格
  - 配置文件：滑杆/配置项风格
  - 代码/脚本：终端窗口风格
  - 文档：文档 + 放大镜风格
  - 文本：多行文本风格
  - 音视频：独立媒体图标风格
- 图片文件仍继续显示真实缩略图。


## r73 图标轮廓优化

- 调整文件类型图标的整体轮廓，不再让大多数图标都基于同一张长方形文件纸。
- 软件包图标（APK/IPK）改为独立的立方体/方盒子风格，更符合“箱子应该是正方形”的视觉预期。
- 压缩包、配置、代码、音频、视频等类别也改为更独立的轮廓样式。
- 文本文档和常规文档继续保留纸张风格，便于和其它类别区分。


## r74 固件内置开机启动修复

- 修复将 QuickFile-Go 编译进新固件后，首次启动或恢复配置后页面显示 `Failed to fetch`、后端未监听 8989 的问题。
- 根因是服务采用 `listen_addr=auto` 绑定 LAN 地址时，启动时序可能早于 `br-lan` 获得地址，导致后端启动失败。
- 启动脚本现在会在 LAN 地址尚未就绪时安全等待，并监听 `lan` 接口事件自动重新启动后端。
- 增加 QuickFile-Go 与 network 配置变更触发，LAN 地址变更后可重新绑定正确地址。
- 保持仅绑定 LAN 地址的安全策略，不改为监听所有接口。


## r75 终端中文 UTF-8 流式解码修复

- 修复终端粘贴或输出中文时偶发显示为 `�` / `���` 的问题。
- 根因是 PTY 二进制 WebSocket 输出分帧后，前端为每个帧单独创建 UTF-8 解码器；中文字符跨帧时会被错误替换。
- 前端终端连接现在复用同一个 `TextDecoder` 并启用流式解码，保留跨帧的 UTF-8 不完整字符。
- 保留 r74 的 LAN 就绪后自动拉起服务修复。


## r76 终端与文本编辑器桌面化窗口增强

- 基于 r75，为实时终端和文本编辑器加入拖动、最小化、最大化 / 恢复交互。
- 支持双击标题栏切换最大化状态；终端最小化时连接保持运行。
- 保留 r75 的终端 UTF-8 中文跨帧解码修复，以及编辑器 Monaco 失败回退逻辑。
- 补充深色 / 浅色模式下的窗口控制按钮与最小化显示样式。


## r77 编辑器语言增强与文件全选

- 编辑器补充常用 Monaco 高亮：TypeScript、Rust、PowerShell、Java、Swift，并启用已随包提供的 Dockerfile 识别。
- Monaco 官方/原版资源集没有独立 TOML 模块，因此为 `.toml` 增加 QuickFile-Go 本地轻量高亮规则，不依赖网络资源。
- 新增文件列表“全选 / 取消全选”按钮；列表模式表头增加全选复选框。
- 批量复制、剪切、删除按钮显示当前选中数量。
- 搜索过滤时自动移除已隐藏项目的选择状态，防止批量操作误处理不可见文件。
- 保留 r76 桌面化终端/编辑器窗口、r75 终端中文修复和 r74 开机启动修复。


## r78 原版风格鼠标框选

- 修正 r77 将“全选”误放入工具栏的交互方向：移除工具栏“全选 / 取消全选”按钮。
- 移植原版文件管理器风格的鼠标拖动框选：在文件区域空白处按住并拖动，蓝色选区覆盖到的项目会被选中。
- 列表模式继续保留表头复选框，符合原版列表批量选择方式。
- 支持 `Ctrl` / `Command` + 拖动在已有选择基础上追加框选。
- 框选只从空白区域启动，不干扰点击打开文件、复选框、右键菜单、拖拽上传和返回上一级。
- 保留 r77 的 Monaco 扩展语言支持和批量操作选择数量提示。


## r79 原版风格紧凑列表布局

- 文件管理默认视图改为列表模式，保留右上角切换为网格视图的能力。
- 按原版 QuickFile 的信息密度优化列表结构：复选框、名称、大小、修改时间、权限、所有者、操作按列显示。
- 目录行显示紧凑小图标和蓝色名称；单行高度降低，一屏可查看更多项目。
- 新增行内操作按钮：重命名、修改权限、删除，减少依赖右键菜单。
- 保留 r78 的鼠标框选和列表表头复选框，保留 r77 Monaco 语言增强及此前稳定性修复。


## r80 默认网格恢复与原版风格列表保留

- 修正 r79 对需求的误解：打开 QuickFile-Go 后默认恢复为图标网格视图。
- 用户切换到列表视图时，继续保留 r79 已实现的原版风格紧凑列表布局：名称、大小、修改时间、权限、所有者、操作按列显示。
- 保留列表模式表头复选框、行内操作按钮和 r78 鼠标拖动框选能力。
- 不修改 Monaco 语言增强、窗口桌面化、终端 UTF-8 修复、开机启动修复或后端文件 API。


## r81 今日改动审计修复

- 修复桌面化窗口启用最小化后，多个编辑器/终端窗口会同时响应 `Esc`，多个编辑器可能同时响应 `Ctrl+S` 的问题。
- 修复终端窗口在 xterm.js 异步加载期间被关闭后，仍可能在后台建立隐藏 PTY WebSocket 连接的问题。
- 修复 `Dockerfile.*` 与 `.mk` 文件虽然具备编辑器识别逻辑，但从文件列表点击不能直接进入编辑器的问题。
- 列表视图“所有者”列由 UID/GID 数字优化为可解析的账号/组名称，例如 `root/root`；无法解析时回退为数字 ID。
- 保留 r80 默认网格、原版风格列表、鼠标框选、Monaco 语言增强、终端中文与开机启动修复。


## r82 列表复选框与浅色模式修复

- 修复原版风格列表 UI 中表头全选框与文件行复选框横向不对齐的问题：数据行改用与表头一致的专用选择列容器。
- 为列表复选框实现 QuickFile-Go 自有样式，避免 LuCI 主题在深色/浅色模式下渲染为黑色方块或位置漂移。
- 补齐浅色列表背景、选择态、复选框、悬停态与行内操作按钮适配。
- 将列表右侧的 Emoji 行内操作图标替换为随主题颜色变化的 SVG 图标，避免浅色模式下锁图标颜色失控。
- 保留默认网格视图、原版风格列表、鼠标框选、编辑器语言增强、终端中文修复与开机启动修复。


## r83 原版复选框与名称列布局修复

- 复选框恢复为原版风格的 14px 描边小方框，浅色模式不再显示黑色实心未选中框。
- 列表模式将复选框、文件图标、名称合并回同一个名称单元格，间距和对齐方式更接近原版。
- 深色主题使用透明未选中框与浅蓝选中态，浅色主题使用白底灰边与蓝色选中态。


## r84 复选框渲染与网格选择状态修复

- 列表复选框改用隐藏状态输入框 + 独立视觉标记，避免 LuCI/浏览器样式覆盖导致浅色模式仍出现黑色方块。
- 列表选中状态统一为蓝底白勾，半选状态为蓝底白横线。
- 网格模式按照原版交互隐藏可见复选框，仅通过框选后的卡片高亮显示选择状态。
- 清理选择状态重复同步残留。

## r85 图片缩略图与浅色工具栏适配修复

- 修复图片缩略图在浅色模式下因沿用深色边框、背景与阴影而显示明显黑框的问题。
- 普通图片缩略图改为透明背景、弱化边界、移除额外阴影；浅色模式使用浅灰边界。
- 深色模式同步弱化缩略图边缘；SVG 缩略图保留易识别的浅色承载背景。
- 修复宽屏下搜索框掉到工具栏下一行的问题；桌面布局中保持搜索框位于操作按钮右侧，小屏仍可换行。
- 修复浅色模式搜索框文字与占位提示过浅的问题，输入文字恢复清晰深色显示。
- 本次不变更选择、列表数据结构、终端、编辑器、启动或后端逻辑。


## r86 今日功能审计修复

- 修复 QuickFile-Go 通过自身安装弹窗升级时，包安装脚本过早重启后端而打断实时安装日志返回的竞态。
- postinst 在已有后端进程运行时不再强行重启；安装 API 确认自身后端二进制已变化后，才在成功日志返回阶段安排延迟重启。
- 修复 `Makefile` / `*.mk` 已能打开编辑器但没有真实 Monaco 高亮实现的问题，新增离线轻量 Makefile tokenizer。
- 延续 r85 的图片缩略图、搜索框浅色适配，以及此前选择框、列表、终端和编辑器修复。


## r87 终端本地资源加载稳定性修复

- 修复打开 Monaco 编辑器后，再打开终端时 `xterm.js` 经常加载失败的问题。
- 根因是 Monaco 注册 AMD 加载器后，UMD 格式的 xterm.js 不再导出 `window.Terminal`；r87 在 AMD 环境中改为按模块方式载入并接回终端。
- 终端加载失败界面增加“重新加载终端资源”按钮，暂时性故障无需刷新整个页面。
- 不修改文件操作 API、安装流程、列表布局、选择逻辑、编辑器窗口或启动安全范围。


## r88 最小化窗口与诊断弹窗修复

- 终端和文本编辑器最小化后，标题栏仍可拖动移动；点击恢复按钮后回到原窗口大小与位置。
- 设置/诊断弹窗增加可视高度限制与内部滚动，避免诊断结果展开后关闭按钮被顶出屏幕。
- 诊断结果新增“收起诊断”入口，同时支持 Esc 和点击遮罩关闭设置窗口。
- 将“读取配置”明确为“重新读取”：重新从 `/etc/config/quickfile-go` 读取已保存配置，会覆盖窗口内尚未保存的输入。
- 修复文本编辑器在 Monaco 异步加载完成前输入内容会被初始文件内容覆盖的问题。


## r89 任务与弹窗交互修复

- 修复 APK/IPK 安装弹窗底部状态和按钮遮挡，日志区独立滚动。
- 自升级时先回传最终成功状态与完整日志，再安排 QuickFile-Go 服务重启，避免状态被重启中断。
- 安装过程可以收起窗口，并在“任务”面板查看状态与完整日志。
- 上传操作改为当前页面后台任务，不再使用阻塞页面且无法关闭的前台进度层；任务面板可查看进度与取消上传。
- 任务面板实时刷新并合并展示后端任务与当前页面上传/安装任务。
- 移除设置中的“重新读取”按钮，减少误解。
- 修复重复选择同一文件上传时不触发文件选择的问题。

注意：浏览器本地上传依赖当前页面持续保持打开；刷新或离开 QuickFile-Go 页面会中断仍未完成的上传任务。


## r90 任务提示风格与自动消失修复

- 上传加入后台任务的提示不再使用 LuCI 顶部长条通知，改为 QuickFile-Go 自己的右上角小型浮动提示。
- 浮动提示适配深色/浅色模式，支持直接打开“任务”面板或手动关闭。
- 已加入任务、上传完成、上传失败、上传取消提示都会按状态显示，并在短时间后自动消失，不再长期占据页面顶部。
- 任务面板中的历史任务记录仍保留，由用户通过“清除已完成记录”管理，不影响追踪结果。
